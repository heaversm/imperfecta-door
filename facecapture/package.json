{
  "name": "facecapture",
  "version": "1.0.0",
  "private": true,
  "description": "Tablet face-collector: capture a face, remove the background on Cloudflare, drop it into a live feed.",
  "scripts": {
    "dev": "wrangler dev",
    "dev:ui": "node scripts/dev.mjs",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/containers": "^0.0.27",
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.20.0"
  }
}
